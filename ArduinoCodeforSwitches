const int buttonPin = 2; //Variables that wont change

int buttonState = 0; //Variables that will change

void setup() {
Serial.begin(9600);
pinMode(buttonPin, INPUT);
}

void loop() {
  digitalRead(buttonPin);
  buttonState = digitalRead(buttonPin);
  Serial.println(buttonState);

}
